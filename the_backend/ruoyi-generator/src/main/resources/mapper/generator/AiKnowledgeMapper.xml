<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 命名空间必须与Mapper接口全路径一致 -->
<mapper namespace="com.ruoyi.generator.mapper.AiKnowledgeMapper">

    <!-- 根据分类查询 -->
    <select id="selectByCategory" resultType="com.ruoyi.generator.domain.AiKnowledge">
        SELECT id, title, content, file_url, category, create_time, update_time
        FROM ai_knowledge
        <where>
            <if test="category != null and category != ''">
                AND category = #{category}
            </if>
        </where>
        ORDER BY create_time DESC
    </select>

    <!-- 批量查询 -->
    <select id="selectByIds" resultType="com.ruoyi.generator.domain.AiKnowledge">
        SELECT id, title, content, file_url, category, create_time, update_time
        FROM ai_knowledge
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

</mapper>